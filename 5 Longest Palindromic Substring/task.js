//给定一个字符串s，找到s中最长的回文子字符串。
// 您可以假设s的最大长度为1000。


/**
 * @param {string} s
 * @return {string}
 */
var longestPalindrome = function(s) {
    let
};

var str = "babad";
console.log(longestPalindrome(str));